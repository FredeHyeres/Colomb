[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.railway"

[deploy]
startCommand = "python demo_init.py && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/config/status"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
